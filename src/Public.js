//사용자 로그인 여부와 상관없이 볼 수 있는 기본 경로.
//단순히 경로 이름을 UI에 렌더링.

import React from 'react';
import Container from './Container';

const Public = () => {
    <Container>
        <h1>Public Router</h1>
    </Container>
}

export default Public;